services:
  - type: web
    name: photo-album
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: photo-uploads
      mountPath: /opt/render/project/src/uploads
      sizeGB: 1
